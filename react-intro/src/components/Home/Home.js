import React from "react";
// import { Redirect } from "react-router-dom";

const Home = () => {

        // if (!isAuth) return < Redirect to="/" />

        return(
        <div>
            <h1>LOL</h1>
        </div>
)};

export default Home;